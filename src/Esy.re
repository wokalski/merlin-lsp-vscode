/*
 
  var exec = require('child_process').exec;
  /

let execEnv = () => {
exec([|"esy", "execEnv"|], {
  cwd: '/home/user/directory'
}, function(error, stdout, stderr) {
  // work with result
});


};
*/
